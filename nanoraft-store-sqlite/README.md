# NanoRaft SQLite store implementation

Gradle (version catalog)

```toml
[versions]
nanoraft-store-sqlite = "0.9"

[libraries]
nanoraft-store-sqlite = { module = "io.nanoraft:nanoraft-store-sqlite", version.ref = "nanoraft-store-sqlite" }
```

Gradle (kotlinscript)

```kotlin
implementation("io.nanoraft:nanoraft-store-sqlite:0.9")
```

Maven

```xml
<dependency>
    <groupId>io.nanoraft</groupId>
    <artifactId>nanoraft-store-sqlite</artifactId>
    <version>0.9</version>
</dependency>
```